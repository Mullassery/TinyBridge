[package]
name    = "tinybridge-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde           = { workspace = true }
serde_yaml      = { workspace = true }
serde_json      = { workspace = true }
thiserror       = { workspace = true }
semver          = { workspace = true }
uuid            = { workspace = true }
chrono          = { workspace = true }
dirs            = { workspace = true }
anyhow          = { workspace = true }
tracing         = { workspace = true }

[features]
# Not implemented today -- gate scaffolding in otel_provider.rs for a future
# real OpenTelemetry/Prometheus integration (see that file's doc comment).
# Declared so `cfg(feature = "otel"/"metrics")` isn't flagged as referencing
# an unknown feature; enabling either does nothing yet.
otel = []
metrics = []
